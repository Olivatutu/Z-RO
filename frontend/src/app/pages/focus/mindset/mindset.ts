import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-mindset',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './mindset.html',
  styleUrls: ['./mindset.scss'],
})
export class MindsetComponent {}
